import { Box, Typography } from '@mui/material';

const Learn = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h3" color="primary">Learn</Typography>
    <Typography variant="body1" sx={{ mt: 2 }}>
      This is the new Learn page. Add your features here!
    </Typography>
  </Box>
);

export default Learn; 